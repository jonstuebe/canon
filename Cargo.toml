[package]
name = "canon"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"  # LazyLock (1.80), Option::is_none_or (1.82)
description = "Rename TV episode files to a canonical \"Show Name SxxEyy.ext\" form"
license = "MIT"

[dependencies]
regex = "1"

[profile.release]
lto = true
strip = true
